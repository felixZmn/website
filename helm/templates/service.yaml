apiVersion: v1
kind: Service
metadata:
  name: {{ include "bentobox.fullname" . }}
  labels:
    {{- include "bentobox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bentobox.selectorLabels" . | nindent 4 }}
